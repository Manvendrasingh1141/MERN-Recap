const Comp3 = ({props}) => {
  return (
    <div>
        <div>Name: {props.name}</div>
        <div>Alias: {props.alias}</div>
        <div>Age: {props.age}</div>
    </div>
  )
}

export default Comp3